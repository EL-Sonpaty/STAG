<UserControl x:Class="STAG.Views.STAGViewLegacy"
             xmlns:fas="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="Auto"
             Width="Auto"
>
    <!--<StackPanel Margin="10">
        --><!-- Stage Input --><!--
        <TextBlock Text="Define your stage:" Margin="0,0,0,5"/>

        <DockPanel>
            <TextBox x:Name="StageInput"
                     FontSize = "12"
                     Width="200"
                     Height="25"
                     VerticalContentAlignment="Center"
                     Margin="0,0,5,0"/>

            <Button Width="25" Height="25" Click="AddStage_Click"
                    Background="#EEE" BorderBrush="Gray" Padding="0">
                <Viewbox Stretch="Uniform">
                    <Grid Width="20" Height="20">
                        <fas:IconBlock Icon="Plus" Foreground="Black"/>
                    </Grid>
                </Viewbox>
            </Button>
        </DockPanel>

        --><!-- Stage List --><!--
        <TextBlock Text="Stages:" Margin="0,10,0,5"/>

        --><!-- Scrollable ListBox for up to 8 items --><!--
        <ScrollViewer VerticalScrollBarVisibility="Auto" Height="520">
            <ListBox x:Name="StageListBox"
                     BorderThickness="1"
                     BorderBrush="LightGray"
                     Padding="4"
                     ScrollViewer.VerticalScrollBarVisibility="Disabled"/>
        </ScrollViewer>
    </StackPanel>-->
</UserControl>